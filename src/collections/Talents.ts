import type { CollectionConfig } from 'payload'

export const Talents: CollectionConfig = {
  slug: 'talents',
  admin: {
    useAsTitle: 'name',
  },
  access: {
    read: () => true,
  },
  fields: [
    {
      name: 'name',
      type: 'text',
      required: true,
    },
    {
      name: 'position',
      type: 'text',
      required: true,
    },
  ],
}
